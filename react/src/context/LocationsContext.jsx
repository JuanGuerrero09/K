import { createContext, useContext, useState, useEffect } from "react";

const LocationContext = createContext({});

export const LocationsProvider({children}) =>{
  const const [locations, setlocations] = useState(second)
}

export interface Location {
  site_name: string;
  site: string;
  geoCode: [number, number];
  points: number;
  category: string;
  is_complete: boolean;
  date_of_completion: string;
  is_unlocked: boolean;
}
